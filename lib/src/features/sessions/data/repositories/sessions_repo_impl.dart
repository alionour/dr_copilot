import 'package:dr_copilot/src/features/sessions/data/remote/session_firebase_api.dart';

class SessionsRepositoryImpl {
  final SessionFirebaseApi sessionFirebaseApi;

  SessionsRepositoryImpl(this.sessionFirebaseApi);

  // Define methods to interact with SessionFirebaseApi
}
