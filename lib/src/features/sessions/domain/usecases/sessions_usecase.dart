import 'package:dr_copilot/src/features/sessions/data/repositories/sessions_repo_impl.dart';

class SessionsUseCase {
  final SessionsRepositoryImpl sessionsRepository;

  SessionsUseCase(this.sessionsRepository);

  // Define methods to interact with SessionsRepositoryImpl
}
