import 'package:flutter_test/flutter_test.dart';
import 'package:dr_copilot/src/features/auth/auth_injections.dart';

void main() {
  test('authInjections can be called without error', () {
    // This is a placeholder; actual test would check DI registration
    expect(() => initAuthInjections(), returnsNormally);
  });
}
