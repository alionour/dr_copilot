{
  "name": "dr-copilot-invitation-backend",
  "version": "1.0.0",
  "description": "AWS Lambda function for sending invitation emails via Amazon SES",
  "main": "index.js",
  "scripts": {
    "install-deps": "npm install",
    "package": "zip -r function.zip .",
    "package-windows": "powershell Compress-Archive -Path * -DestinationPath function.zip -Force"
  },
  "keywords": [
    "aws",
    "lambda",
    "ses",
    "invitation",
    "dr-copilot"
  ],
  "author": "dr_copilot",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-ses": "^3.58.0"
  }
}
